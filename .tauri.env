# Tauri signing keys (set as environment variables, never commit real values)
# Generate keys with: npm run tauri signer generate -- -w ~/.tauri/arki3d.key
# TAURI_SIGNING_PRIVATE_KEY=<base64-encoded-private-key>
# TAURI_SIGNING_PRIVATE_KEY_PASSWORD=
