# App Icons

Place app icons here before building.

Required files:

- `32x32.png`
- `128x128.png`
- `128x128@2x.png`
- `icon.icns` (macOS)
- `icon.ico` (Windows)

Generate with:

```bash
npm run tauri icon path/to/source-icon.png
```
